<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{pageTitle}}</title>
    <style>
       body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f8f9fa;
}

.background-container {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url('images/N3DS_PokemonSuperMysteryDungeon_MainIllustration_png_jpgcopy.0.0.jpg');
    background-size: cover;
    background-position: center;
    z-index: -1;
    filter: brightness(0.8) contrast(1.2) saturate(1.2);
}

nav {
    text-align: center;
    margin-top: 20px;
}

nav ul {
    list-style-type: none;
    padding: 0;
}

nav ul li {
    display: inline;
    margin: 0 10px;
}

nav ul li a {
    text-decoration: none;
    padding: 10px 20px;
    border-radius: 5px;
    transition: background-color 0.3s ease;
    color: #fff;
    background-color: #007bff;
}

nav ul li a:hover {
    background-color: #0056b3;
}

/* Styles for individual buttons */
.decks-button, .teams-button, .favorites-button, .logout-button {
    display: inline-block;
    padding: 15px 30px;
    margin: 10px;
    border-radius: 10px;
    border: none;
    background-color: #030304;
    color: #f2960b;
    font-size: 18px;
    text-decoration: none;
    transition: transform 0.2s, background-color 0.3s;
}

.decks-button:hover, .teams-button:hover, .favorites-button:hover, .logout-button:hover {
    background-color: #797478;
    transform: translateY(-2px);
}

/* Additional styles for individual buttons */
.decks-button::before, .teams-button::before, .favorites-button::before, .logout-button::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: #fff;
    z-index: -1;
    border-radius: 10px;
    transition: opacity 0.3s, transform 0.3s;
    opacity: 0;
    transform: scale(0.9);
}

.decks-button:hover::before, .teams-button:hover::before, .favorites-button:hover::before, .logout-button:hover::before {
    opacity: 1;
    transform: scale(1);
    background-color: #797478;
}

/* Styles for the login box */
.login-container {
    max-width: 400px;
    margin: 50px auto;
    padding: 30px;
    background-color: #797478(255, 255, 255, 0.9);
    border-radius: 10px;
    box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
}

.login-container h1 {
    text-align: center;
    margin-bottom: 20px;
    color: #333;
    font-size: 24px;
}

.login-form {
    display: flex;
    flex-direction: column;
}

.login-input {
    margin-bottom: 20px;
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 5px;
}

.login-button {
    padding: 15px;
    background-color: #050506;
    color: #f2960b;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

.login-button:hover {
    background-color: #797478;
}

.error-message {
    color: red;
    text-align: center;
    margin-top: 10px;
}
    </style>
</head>
<body>
    <div class="background-container"></div>
    <header>
        <h1 style="font-family: 'Arial', sans-serif; color: #030303; text-align: center; margin-top: 50px; font-size: 48px; text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2); background-color: #ecf0f1; padding: 20px; border-radius: 10px; box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);">Welcome to {{pageTitle}}</h1>

        <nav>
            <ul>
                <li><a href="#" class="decks-button">Decks</a></li>
                <li><a href="#" class="teams-button">Teams</a></li>
                <li><a href="#" class="favorites-button">Favorites</a></li>
                <li><a href="#" class="logout-button">Logout</a></li>
            </ul>
        </nav>
    </header>
    <main class="login-container">
        <h1>Login</h1>
        <form class="login-form" id="login-form">
            <input type="email" class="login-input" placeholder="Email" required>
            <input type="password" class="login-input" placeholder="Password" required>
            <button type="submit" class="login-button">Login</button>
            <p class="error-message" id="error-message">{{errorMessage}}</p>
        </form>
    </main>
    <footer>
        <p>{{currentYear}}</p>
    </footer>
    <script src="{{scriptSrc}}"></script>
</body>
</html>


